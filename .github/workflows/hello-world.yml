name: Hello World

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - hello-world/**

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Say hello
        run: |
          echo "Hello from GitHub Actions!"
