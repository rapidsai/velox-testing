name: Presto Test Composite
description: Template for Presto jobs
on:
  workflow_call:
    inputs:
      velox_commit:
        description: 'Velox commit SHA or branch'
        type: string
        required: false
        default: 'merged-prs'
        #default: 'main'
      presto_commit:
        description: 'Presto commit SHA or branch'
        type: string
        required: false
        default: 'f31246e1ecce7cc1ea6b2fff34a0dc701fa9c5a9'
        #default: 'master'
      presto_worker_type:
        description: 'Type of presto worker to use'
        type: string
        required: true
      node_label:
        description: 'Node label to run on'
        type: string
        required: true

jobs:
  build-and-test:
    runs-on: ${{ inputs.node_label }}
    steps:
      - name: Checkout velox-testing
        uses: actions/checkout@v4
        with:
          path: velox-testing
      - name: Checkout Velox
        uses: actions/checkout@v4
        with:
          # Move this back to facebookincubator once PRs are merged upstream.
          #repository: facebookincubator/velox
          repository: rapidsai/velox
          ref: ${{ inputs.velox_commit }}
          path: velox
      - name: Checkout Presto
        uses: actions/checkout@v4
        with:
          repository: prestodb/presto
          ref: ${{ inputs.presto_commit }}
          path: presto
      - name: Patch Presto
        run: |
          pushd presto
          git apply ../velox-testing/presto/install_gperf.patch
          popd
      - name: Patch Velox
        run: |
          pushd velox
          git apply ../velox-testing/velox/merged-prs_091025.patch
          popd
      - name: Build and run presto
        run: |
          pushd velox-testing/presto/scripts
          ./start_${{ inputs.presto_worker_type }}_presto.sh
          ./run_integ_test.sh -b tpch
          popd
      - name: Shutdown
        run: |
          pushd velox-testing/presto/scripts
          ./stop_presto.sh
          popd
