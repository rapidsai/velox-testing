name: Presto Nightly Test
description: Runs Presto Test nightly for all configurations
on:
  # schedule:
  #   - cron: '0 4 * * *'  # daily at 04:00 UTC
  workflow_dispatch:
  #[push]

jobs:
  nightly:
    uses: ./.github/workflows/presto-test.yml
    with:
      run_java_tests: true
      run_cpu_tests: true
      run_gpu_tests: true

