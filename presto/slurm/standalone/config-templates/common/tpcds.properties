# Select the built-in TPC-DS connector that generates synthetic datasets on the
# fly. Used for performance testing and benchmarking without external storage.
connector.name=tpcds

# Use VARCHAR instead of CHAR types for generated columns. Required for native
# execution as the native engine doesn't support CHAR type semantics.
tpcds.use-varchar-type=true
