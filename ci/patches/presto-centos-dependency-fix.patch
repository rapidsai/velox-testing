--- a/scripts/dockerfiles/centos-dependency.dockerfile
+++ b/scripts/dockerfiles/centos-dependency.dockerfile
@@ -1,5 +1,11 @@
-RUN bash -c "mkdir build && \
-    (cd build && echo 'Original build step placeholder') && \
-    rm -rf build"
+RUN bash -c "mkdir build && \
+    (cd build && ../scripts/setup-centos.sh && \
+    source ../velox/scripts/setup-centos9.sh && \
+    source ../velox/scripts/setup-centos-adapters.sh && \
+    install_adapters && \
+    install_clang15 && \
+    install_cuda 12.8) && \
+    rm -rf build"
 
 # Rest of the dockerfile continues...