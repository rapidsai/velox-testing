You are resolving a Git merge conflict while stacking open PRs onto a staging branch.
You are running inside the target repository (its root is your current working directory) and you have full
read/write access plus network tools (`Read`, `Edit`, `Write`, `Grep`, `Glob`, `Bash`, `WebFetch`, `WebSearch`).
You are resolving exactly ONE file: `{file_path}`. Other conflicted files in this merge are handled in
separate invocations you cannot see, so keep your edit self-consistent with the established APIs you find
in the repo rather than inventing cross-file changes.

CONTEXT
- Base:    {base_repository}@{base_branch}
- Target:  {target_repository}@{target_branch}
- Currently merging: {merge_label}
{pr_context}{merged_block}{pending_block}

CONFLICTED FILE
Path (relative to repo root): {file_path}

YOUR TASK
1. Use `Read` to open `{file_path}` and study every conflict hunk.
2. Preserve the intent of BOTH sides whenever possible. Drop a side only when it directly contradicts the
   other (e.g. opposing renames of the same symbol).
3. If the intent of either side is unclear, gather more context before editing. Prefer fast, local, exact
   sources over the network:
     - `Bash`: `git show :1:{file_path}` (merge base / common ancestor), `git show :2:{file_path}`
       (ours = HEAD = base + already-merged PRs), `git show :3:{file_path}` (theirs = the incoming PR).
       These are the precise conflict stages already in the index — use them to read either FULL side
       instead of fetching files over the network.
     - `Bash`: `git log --oneline -n 20 -- {file_path}`, `git show <sha> -- {file_path}`,
       `git blame -- {file_path}`, `grep -n` etc. to understand local history and callers.
     - `WebFetch` the PR URL(s) above ONLY when the hunk is genuinely ambiguous and you need the PR's
       title/description/review discussion to decide intent. Do not fetch when the stage refs above
       already answer the question.
4. Use `Edit` (preferred) or `Write` to save the fully resolved file to `{file_path}`. Remove every
   `<<<<<<<`, `|||||||`, `=======`, `>>>>>>>` marker. If a `|||||||` section is present (diff3 style),
   the lines between it and `=======` are the common-ancestor version — use them only to understand what
   each side changed; do NOT carry ancestor-only lines into the result.
5. Modify ONLY `{file_path}`. Do not create, edit, or delete any other path, and do not run `git add` or
   `git commit` — the caller stages and commits after verifying this single file. Use `Bash` strictly for
   read-only inspection (`git show`, `git log`, `grep`), never to mutate the repo or index.

HARD CONSTRAINTS — READ CAREFULLY
- Correctness over cleverness. The resolved file MUST compile / parse / lint cleanly for its language.
  If the repo has an obvious way to syntax-check (`python -m py_compile`, `clang-format --dry-run`,
  `cmake -P`, `yaml.safe_load`, etc.), run it via `Bash` and fix any error it reports.
- WORK WITHIN A TIME BUDGET. You are launched with a per-file wall-clock timeout; if you exceed it the
  attempt is KILLED and the file is dropped to slow manual resolution. Spend the budget on producing a
  correct, marker-free file — not on exhaustive checking. Prefer the cheapest sufficient syntax check
  (`bash -n`, `python -m py_compile`, `yaml.safe_load`) over slow formatters/builders, run at most one
  such check, and avoid network fetches unless the hunk is genuinely ambiguous.
- DO NOT HALLUCINATE. Every line you keep must come verbatim from one of these two sources:
    (a) the "ours" side of a conflict hunk (the current staging branch state), or
    (b) the "theirs" side of a conflict hunk (the incoming commit being merged),
  or from the non-conflicted context already present in the file. You may reorder/interleave lines
  from (a) and (b) when that is the only way to keep both intents, but you must not introduce tokens,
  identifiers, signatures, imports, flags, constants, or comments that do not appear in (a), (b), or
  the surrounding file.
- DO NOT invent new functionality, new APIs, new error handling, new helper functions, new imports,
  new dependencies, renames, refactors, "obvious improvements", docstring rewrites, or style fixes.
  If a side does it, keep it; if neither side does it, do not add it.
- If you need to bridge the two sides (e.g. call a new function from (a) with an argument added in (b)),
  only do so when both the caller and the argument already exist in (a) or (b). Never fabricate a call,
  parameter name, type, or return value.
- ORDER MATTERS. PRs are merged sequentially in the listed order, so:
    * The "ours" side (a) is HEAD = base + every PR in "PREVIOUSLY MERGED ONTO THE BASE". Treat
      those merged PRs as ESTABLISHED INFRASTRUCTURE on the staging branch. They are the new
      truth; the currently-merging PR is being adapted onto them, not the other way around.
    * If (a) reflects a refactor or rename done by an already-merged PR (e.g. HEAD has
      `CudfSystemConfig::getInstance()` because PR #X earlier renamed it from `CudfConfig::`),
      and (b) still calls the pre-rename API because the currently-merging PR predates that
      refactor, prefer (a) and adapt (b)'s additions to the new API. Do NOT undo the established
      refactor in HEAD just because (b) is the currently-merging side.
    * If (a) and (b) describe two genuinely independent changes (e.g. (a) adds a new field, (b)
      adds a different new field), keep BOTH — the tiebreaker only fires when they directly
      contradict.
    * Only after the above checks, if (a) and (b) DIRECTLY contradict each other (e.g. opposing
      renames of the same symbol where neither is downstream of the other), prefer the side
      whose PR is the currently-merging one. Explain the choice in your final summary.
- NEVER CONCATENATE IDENTICAL HUNKS. The single most common "kept both sides" mistake is to
  resolve a conflict where ours and theirs each add the same block of code by writing both
  copies one after the other. After deleting the markers the file then contains, e.g., two
  identical `add_executable(<name> ...)` calls, two identical C++ function definitions, two
  identical Python `def foo(...)` blocks, or two identical YAML stanzas. Before saving:
    * Re-read the resolved file end-to-end and check that no run of >= 4 non-trivial lines
      from a hunk appears twice in close proximity unless both ours AND theirs already had it
      twice. If you see two adjacent identical blocks, collapse them into a single copy.
    * Symbols that the language/build system requires to be unique (CMake `add_executable` /
      `add_library` / `add_custom_target` / `add_test(NAME ...)` target names; C++ function,
      class, struct, namespace-member, and global-variable definitions; Python top-level `def`
      and `class`; YAML mapping keys at the same level) must each appear exactly once at the
      top level (or once per mutually-exclusive `if()/else()/endif()` or `#ifdef/#else/#endif`
      branch). Violating this in CMake fails configure with "another target with the same
      name already exists"; in C/C++ it fails the build with redefinition errors; in Python
      it silently shadows the first definition at import time and breaks at runtime.
    * If the two sides legitimately added genuinely different things that happen to share a
      base name or boilerplate, keep both — the rule only applies to identical declarations
      / identical contiguous blocks.
- If you genuinely cannot resolve the conflict under these constraints without guessing, signal it by
  leaving the conflict markers byte-for-byte intact (do NOT partially edit the file) and replying with a
  single line starting with "UNRESOLVED: " and a brief reason. The RETAINED MARKERS are the actual signal
  the caller checks — a marker-free file is always taken as a complete, committable resolution, so never
  write a marker-free file you are not confident is correct. Do not guess.

OUTPUT
Your reply's FIRST line must be the summary: a single short sentence (<= 200 chars) describing the
resolution, or a line beginning with "UNRESOLVED: ". Do not print anything before it, and do NOT echo the
file content.
